import {
  TimeSelector,
  VisitorSelector,
  SourceSelector,
} from '../layouts/SelectHeader';

export function RegionalAnalysis() {
  return (
    <div>
      {/* <TimeSelector onChangeTime={} time='today'></TimeSelector>
      <VisitorSelector onChangeVisitor={} visitor='all'></VisitorSelector>
      <SourceSelector onChangeSource={} source='all'></SourceSelector> */}
    </div>
  );
}
